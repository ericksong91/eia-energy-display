import { useState, useEffect, Suspense, lazy } from 'react';

function EIAAPIFetchTest() {
  useEffect(() => {
    const newObj = {}
  }, [])


  return (
    <div className="EIAFetchTest">
      <h1>Help</h1>
    </div>
  );
}

export default EIAAPIFetchTest;
